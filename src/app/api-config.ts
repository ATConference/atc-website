import { environment } from '../environments/environment';

export const ARCHIVES_URL = `${environment.baseUrl}/conferences`;
export const PARTICIPANTS_URL = `${environment.baseUrl}/participants`;
export const PROGRAMS_URL = `${environment.baseUrl}/programs`;
